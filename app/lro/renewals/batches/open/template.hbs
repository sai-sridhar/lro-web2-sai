<div class="batches">
	{{#if model.length}}
		<table>
			<thead>
				<tr>
					<th>Batch/Community Name</th>
					<th>Date Range</th>
					<th>Expirations</th>
					<th class="no-mobile">Approvals</th>
					<th class="no-mobile">Avg. Rec. Rent</th>
					<th>Avg. Increase</th>
					<th class="no-mobile">Avg. Rec. Term</th>
					<th class="no-mobile">Avg. Prior Rent</th>
					<th class="no-mobile">Avg. Prior Term</th>
					<th class="no-mobile">Last Modified</th>
				</tr>
			</thead>
			<tbody>

					{{#each model as |batch|}}
						{{#link-to "lro.renewals.batch" batch.id tagName="tr"}}
							<td>{{batch.name}}</td>
							<td>{{batch.dateRange}}</td>
							<td>{{format-commas batch.expirationCount}}</td>
							<td class="no-mobile">{{format-commas batch.approvalCount}}</td>
							<td class="no-mobile">${{format-commas batch.avgRecRent}}</td>
							<td>{{format-percent batch.avgIncrease}}</td>
							<td class="no-mobile">{{format-commas batch.avgRecLeaseTerm}}</td>
							<td class="no-mobile">${{format-commas batch.avgCurrentRent}}</td>
							<td class="no-mobile">{{format-commas batch.avgCurrentLeaseTerm}}</td>
							<td class="no-mobile">{{batch.modifiedDate}}<br>{{batch.modifiedBy}}</td>
						{{/link-to}}
					{{/each}}


			</tbody>
		</table>
	{{else}}
		<h2 class="center dbl-top-margin">There are no open renewal batches currently.</h2>
	{{/if}}
</div>
